{
  "name": "live-package",
  "version": "0.2.0",
  "description": "Work with your packages (design systems / component libraries etc) locally in your app with live reloading! ",
  "main": "index.js",
  "bin": {
    "live-package": "./cli.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "local-build": "tsc && cp package.json dist && cd dist && yalc publish",
    "build": "rm -rf dist && tsc && cp package.json dist && cp Readme.md dist"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:johot/live-package.git"
  },
  "keywords": [],
  "author": "Johan Otterud <johan.otterud@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^12.11.1",
    "chalk": "^2.4.2",
    "commander": "^3.0.2",
    "concurrently": "^5.0.0",
    "debounce": "^1.2.0",
    "escape-string-regexp": "^2.0.0",
    "find": "^0.3.0",
    "get-all-files": "^1.0.7",
    "inquirer": "^7.0.0",
    "npm-add-script": "^1.1.0",
    "npm-run": "^5.0.1",
    "sync-directory": "^2.1.0"
  }
}
